import React from "react";

const Notification = () => {
    return (
        <div className="flex h-12 font-bold bg-red-500 text-white px-4 items-center justify-center text-sm md:text-base cursor-pointer">Notification</div>
    );
};

export default Notification;